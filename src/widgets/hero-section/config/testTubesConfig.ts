import { TestTubeConfig } from '@/src/entities/test-tube';

/**
 * Конфигурация 3D пробирок
 * Все параметры позиции, поворота и масштаба можно настроить здесь
 */

export interface ContainerConfig {
  width: number;
  height: number;
  cursor: string;
}

export const testTubesConfig: TestTubeConfig[] = [
  {
    id: 'tube-1',
    name: 'Пробирка вакуумная с гепарином',
    modelPath: '/models/test-tube-1.gltf',
    
    // Позиция пробирки: [x, y, z]
    // x: горизонтальное смещение (отрицательное = слева, положительное = справа)
    // y: вертикальное смещение (отрицательное = ниже, положительное = выше)
    // z: глубина (отрицательное = дальше, положительное = ближе к камере)
    position: [-0.4, -0.3, 0.2],
    
    // Поворот пробирки: [x, y, z] в радианах
    // x: наклон вперед/назад
    // y: поворот вокруг вертикальной оси (0.3 = наклон вправо)
    // z: наклон влево/вправо (0.15 = небольшой наклон)
    rotation: [0, 0.3, 0.15],
    
    // Масштаб пробирки (1 = оригинальный размер)
    scale: 10,
    
    // Цвет крышки (серый для первой пробирки)
    capColor: '#808080',
    
    // Аннотация для первой пробирки (снизу)
    annotation: {
      text: 'Пробирка вакуумная\nс гепарином',
      // Позиция текста относительно пробирки (снизу)
      textPosition: [0, -0.8, 0],
      // Начало стрелки (от текста вверх)
      arrowStart: [0, 0.1, 0],
      // Конец стрелки (к пробирке)
      arrowEnd: [0, 0.5, 0],
      // Начало синей линии (над текстом)
      lineStart: [-0.08, 0.05, 0],
      // Конец синей линии
      lineEnd: [0.08, 0.05, 0],
    },
  },
  {
    id: 'tube-2',
    name: 'Пробирка с цитратом натрия 3,2%',
    modelPath: '/models/test-tube-2.gltf',
    
    // Позиция пробирки: [x, y, z]
    // x: справа от центра
    // y: выше центра
    // z: дальше от камеры (сзади первой пробирки)
    position: [0.4, 0.1, -0.2],
    
    // Поворот пробирки: [x, y, z] в радианах
    // y: -0.3 = наклон влево (против часовой стрелки)
    // z: -0.15 = небольшой наклон в другую сторону
    rotation: [0, -0.3, -0.15],
    
    // Масштаб пробирки
    scale: 9.5,
    
    // Аннотация для второй пробирки (сверху)
    annotation: {
      text: 'Пробирка с цитратом\nнатрия 3,2%',
      // Позиция текста относительно пробирки (сверху)
      textPosition: [0, 0.8, 0],
      // Начало стрелки (от текста вниз)
      arrowStart: [0, -0.1, 0],
      // Конец стрелки (к пробирке)
      arrowEnd: [0, -0.5, 0],
      // Начало синей линии (под текстом)
      lineStart: [-0.08, -0.05, 0],
      // Конец синей линии
      lineEnd: [0.08, -0.05, 0],
    },
  },
];

// Настройки контейнера для пробирок
export const containerConfig: ContainerConfig = {
  // Ширина контейнера в пикселях
  width: 1440,
  
  // Высота контейнера в пикселях
  height: 900,
  
  // Курсор при наведении
  cursor: 'pointer',
};
